using UnityEngine;

public class Kinetics : MonoBehaviour {

  // public static void Move(this Vector2 cPos, Vector2 tPos, float cSpeed, float tSpeed) {
  //   Vector2 dir = Vector2.ClampMagnitude((tPos - cPos), tSpeed);
  //   Vector2 acc = dir * Mathf.Max(tSpeed - cSpeed, 0f);
  //   cPos += acc * Time.fixedDeltaTime;
  // }
  //
  // // public static void Move(this Rigidbody2D rb) {
  //
  // public static void Move()
}
